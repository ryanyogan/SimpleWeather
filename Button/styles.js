'use strict';

import React, {
  StyleSheet
} from 'react-native';

const BASE_FONT_SIZE = 16;

const styles = StyleSheet.create({
  button: {
    backgroundColor: '#FFDDFF',
    width: 200,
    padding: 25,
    borderRadius: 5
  }
});

export default styles;
